---
phase: 02-public-content-pages
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - app/about/page.tsx
  - app/resume/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor can view About page with narrative and strengths sections"
    - "Visitor can view Resume page with highlights, experience, education, and skills"
    - "Resume data is rendered from Convex (or shows placeholder if no data)"
  artifacts:
    - path: "app/about/page.tsx"
      provides: "About page with narrative and strengths"
      min_lines: 40
    - path: "app/resume/page.tsx"
      provides: "Resume page with Convex data rendering"
      min_lines: 50
  key_links:
    - from: "app/resume/page.tsx"
      to: "convex/resume.ts"
      via: "fetchQuery"
      pattern: "fetchQuery.*api\\.resume\\.get"
---

<objective>
Build About and Resume pages with static content and Convex-driven data.

Purpose: About page establishes personal narrative and strengths. Resume page displays structured professional data from Convex. Both pages are simpler than home page (no animations, straightforward layouts).

Output: Two complete pages with dark theme styling, responsive layouts, and proper SEO metadata.
</objective>

<execution_context>
@D:\Projects\saad-portfolio\.claude\get-shit-done\workflows\execute-plan.md
@D:\Projects\saad-portfolio\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\Projects\saad-portfolio\.planning\PROJECT.md
@D:\Projects\saad-portfolio\.planning\REQUIREMENTS.md
@D:\Projects\saad-portfolio\.planning\phases\02-public-content-pages\02-RESEARCH.md
@D:\Projects\saad-portfolio\convex\schema.ts
@D:\Projects\saad-portfolio\convex\resume.ts
@D:\Projects\saad-portfolio\app\layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build About page with narrative and strengths</name>
  <files>app/about/page.tsx</files>
  <action>
Replace placeholder About page with static Server Component containing narrative and strengths sections.

RESEARCH PATTERN: About page is static content (no Convex data), so use simple Server Component without preloadQuery.

```typescript
export const metadata = {
  title: "About | Saad Siddiqui",
  description: "Background, approach, and strengths in full-stack development and automation",
};

export default function AboutPage() {
  return (
    <div className="max-w-3xl mx-auto space-y-16 pb-16">
      <section>
        <h1 className="text-4xl font-bold mb-8 text-gray-50">About</h1>

        <div className="space-y-6 text-gray-300 text-lg leading-relaxed">
          <p>
            I'm a full-stack engineer who believes the best code is code that ships fast,
            scales confidently, and maintains itself. My approach combines modern web
            technologies with AI-assisted development workflows to deliver production-ready
            applications faster than traditional methods.
          </p>

          <p>
            This portfolio itself is a demonstration of that philosophy. Built with Next.js,
            TypeScript, Convex, and WorkOS, it showcases an end-to-end automated workflow
            where Claude Code handles everything from issue specification to pull request
            reviews, CI validation, and deployment.
          </p>

          <p>
            My background spans startup environments and product-focused teams where shipping
            quickly and iterating based on real user feedback is essential. I specialize in
            translating ambiguous requirements into working software, establishing development
            workflows that enable teams to move faster, and building systems that scale without
            constant maintenance.
          </p>

          <p>
            When I'm not coding, I'm exploring new tools in the AI-assisted development space,
            contributing to open source projects, and documenting patterns that help other
            engineers ship faster.
          </p>
        </div>
      </section>

      <section>
        <h2 className="text-3xl font-bold mb-8 text-gray-50">Core Strengths</h2>

        <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
          <div>
            <h3 className="text-xl font-semibold mb-3 text-gray-100">
              Full-Stack Development
            </h3>
            <p className="text-gray-400">
              Modern web stack with Next.js, TypeScript, React, and serverless backends.
              Building responsive interfaces and scalable APIs with performance and
              maintainability in mind.
            </p>
          </div>

          <div>
            <h3 className="text-xl font-semibold mb-3 text-gray-100">
              AI-Assisted Workflows
            </h3>
            <p className="text-gray-400">
              Integrating Claude Code and GitHub Actions to automate development workflows.
              Reducing time from idea to production through intelligent automation and
              continuous deployment.
            </p>
          </div>

          <div>
            <h3 className="text-xl font-semibold mb-3 text-gray-100">
              System Design & Architecture
            </h3>
            <p className="text-gray-400">
              Designing systems that grow with product needs. Making architectural decisions
              that balance simplicity, scalability, and developer experience.
            </p>
          </div>

          <div>
            <h3 className="text-xl font-semibold mb-3 text-gray-100">
              Rapid Prototyping
            </h3>
            <p className="text-gray-400">
              Building MVPs and proof-of-concepts quickly to validate ideas. Using modern
              tools and frameworks to deliver working software in days, not months.
            </p>
          </div>
        </div>
      </section>

      <section className="bg-gray-900 rounded-lg p-8 border border-gray-800">
        <h2 className="text-2xl font-bold mb-4 text-gray-50">
          Working with Engineering Managers
        </h2>
        <p className="text-gray-400 leading-relaxed">
          I work best with engineering managers who value velocity, quality, and automation.
          If you're looking for someone who can ship features quickly while maintaining high
          standards, establish development workflows that scale, and bring AI-assisted
          development practices to your team, let's talk.
        </p>
      </section>
    </div>
  );
}
```

CONTENT NOTES:
- Narrative: Establishes "ship fast, scale confidently" positioning
- Self-referential: Portfolio itself is proof-of-concept for automated workflow
- Target audience: Engineering managers (explicit section at end)
- Strengths: Mix of technical skills and workflow optimization

STYLING NOTES:
- max-w-3xl for comfortable reading width
- space-y-16 for section separation
- Responsive grid for strengths (1 col mobile, 2 cols desktop)
- Dark card at bottom for call-out section
  </action>
  <verify>
Visit http://localhost:3000/about in browser.
Verify narrative section with 4 paragraphs renders.
Verify strengths section with 4 cards in 2-column grid renders.
Verify engineering manager call-out section at bottom renders.
Run `npm run typecheck` to verify no TypeScript errors.
  </verify>
  <done>
About page complete with narrative, strengths grid, and engineering manager section. Dark theme styling applied throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Resume page with Convex data rendering</name>
  <files>app/resume/page.tsx</files>
  <action>
Replace placeholder Resume page with Server Component that fetches and renders resume data from Convex.

RESEARCH PATTERN: Use fetchQuery (not preloadQuery) because resume data is static and doesn't need client-side reactivity.

```typescript
import { fetchQuery } from "convex/nextjs";
import { api } from "@/convex/_generated/api";

export const metadata = {
  title: "Resume | Saad Siddiqui",
  description: "Professional experience, skills, education, and highlights",
};

export default async function ResumePage() {
  const resume = await fetchQuery(api.resume.get);

  // Handle case where resume data doesn't exist yet (will be added via admin panel)
  if (!resume) {
    return (
      <div className="max-w-4xl mx-auto py-16">
        <h1 className="text-4xl font-bold mb-8 text-gray-50">Resume</h1>
        <div className="bg-gray-900 rounded-lg p-8 border border-gray-800">
          <p className="text-gray-400">
            Resume data not available yet. Content will be added via admin panel.
          </p>
        </div>
      </div>
    );
  }

  return (
    <div className="max-w-4xl mx-auto space-y-16 pb-16">
      <h1 className="text-4xl font-bold mb-8 text-gray-50">Resume</h1>

      {/* Highlights Section */}
      {resume.highlights.length > 0 && (
        <section>
          <h2 className="text-2xl font-bold mb-6 text-gray-50">Highlights</h2>
          <ul className="space-y-3">
            {resume.highlights.map((highlight, i) => (
              <li key={i} className="flex items-start">
                <span className="text-gray-500 mr-3 mt-1">→</span>
                <span className="text-gray-300 text-lg">{highlight}</span>
              </li>
            ))}
          </ul>
        </section>
      )}

      {/* Experience Section */}
      {resume.experience.length > 0 && (
        <section>
          <h2 className="text-2xl font-bold mb-6 text-gray-50">Experience</h2>
          <div className="space-y-8">
            {resume.experience.map((exp, i) => (
              <div key={i} className="border-l-2 border-gray-800 pl-6">
                <h3 className="text-xl font-semibold text-gray-50">{exp.role}</h3>
                <div className="text-gray-400 mb-2">
                  {exp.company} • {exp.period}
                </div>
                <p className="text-gray-400 mb-4">{exp.description}</p>
                {exp.achievements.length > 0 && (
                  <ul className="space-y-2">
                    {exp.achievements.map((achievement, j) => (
                      <li key={j} className="flex items-start">
                        <span className="text-gray-600 mr-2">•</span>
                        <span className="text-gray-400">{achievement}</span>
                      </li>
                    ))}
                  </ul>
                )}
              </div>
            ))}
          </div>
        </section>
      )}

      {/* Education Section */}
      {resume.education.length > 0 && (
        <section>
          <h2 className="text-2xl font-bold mb-6 text-gray-50">Education</h2>
          <div className="space-y-4">
            {resume.education.map((edu, i) => (
              <div key={i}>
                <h3 className="text-lg font-semibold text-gray-50">{edu.degree}</h3>
                <div className="text-gray-400">
                  {edu.institution} • {edu.year}
                </div>
              </div>
            ))}
          </div>
        </section>
      )}

      {/* Skills Section */}
      {resume.skills.length > 0 && (
        <section>
          <h2 className="text-2xl font-bold mb-6 text-gray-50">Skills</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            {resume.skills.map((skillGroup, i) => (
              <div key={i}>
                <h3 className="font-semibold mb-3 text-gray-100">
                  {skillGroup.category}
                </h3>
                <div className="flex flex-wrap gap-2">
                  {skillGroup.items.map((skill) => (
                    <span
                      key={skill}
                      className="px-3 py-1 bg-gray-900 border border-gray-800
                                 rounded text-sm text-gray-300"
                    >
                      {skill}
                    </span>
                  ))}
                </div>
              </div>
            ))}
          </div>
        </section>
      )}
    </div>
  );
}
```

RESEARCH PATTERN: Server Component with fetchQuery for static data. Conditional rendering handles empty state gracefully.

DATA HANDLING:
- Empty state message if no resume data exists yet
- Conditional rendering for each section (highlights, experience, education, skills)
- Maps over Convex data arrays to render dynamic content

STYLING NOTES:
- max-w-4xl for comfortable reading on wide screens
- border-l-2 for experience timeline visual
- Responsive grid for skills (1 col mobile, 2 cols desktop)
- Consistent gray scale for hierarchy
  </action>
  <verify>
Visit http://localhost:3000/resume in browser.
Verify empty state message shows (expected - no resume data in Convex yet).
Run `npm run typecheck` to verify no TypeScript errors.
Check that fetchQuery is imported from "convex/nextjs".
  </verify>
  <done>
Resume page complete with Convex data rendering and empty state handling. Will display structured resume when data is added via admin panel.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Two complete pages:
1. About page with narrative (4 paragraphs) and strengths grid (4 cards)
2. Resume page with empty state placeholder (will show data when added via admin panel)
  </what-built>
  <how-to-verify>
1. Visit http://localhost:3000/about
   - Verify narrative section renders with 4 paragraphs
   - Verify strengths grid shows 4 cards in 2 columns (desktop) or 1 column (mobile)
   - Verify engineering manager call-out section at bottom
   - Check dark theme styling (gray-950 bg, gray-50 text)

2. Visit http://localhost:3000/resume
   - Verify empty state message: "Resume data not available yet"
   - Verify dark theme styling consistent with About page

3. Test responsive layout
   - Resize browser to 375px (mobile) - verify 1 column layout
   - Resize to 1440px (desktop) - verify 2 column strengths grid

4. Check console for errors (none expected)

EXPECTED STATE: Both pages render correctly with empty state for resume (data will be populated via admin panel in Phase 4).
  </how-to-verify>
  <resume-signal>
Type "approved" to continue, or describe any issues to address.
  </resume-signal>
</task>

</tasks>

<verification>
Visit http://localhost:3000/about and verify:
- Narrative section with 4 paragraphs
- Strengths grid with 4 cards (responsive)
- Engineering manager call-out section
- Dark theme styling

Visit http://localhost:3000/resume and verify:
- Empty state message (expected)
- Dark theme styling
- No console errors
</verification>

<success_criteria>
1. About page renders narrative and strengths sections
2. About page has responsive 2-column grid for strengths
3. Resume page fetches data from Convex using fetchQuery
4. Resume page shows empty state when no data exists
5. Both pages use consistent dark theme styling
6. Both pages have proper SEO metadata
7. Responsive layouts work on mobile and desktop
8. No TypeScript or runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-content-pages/02-03-SUMMARY.md` documenting:
- About page content and structure
- Resume page Convex integration
- Empty state handling approach
- Human verification results
- Any content or styling adjustments
</output>
